<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tb="http://www.hardcodet.net/taskbar"
                    xmlns:local="clr-namespace:Unmute.App.WPF.UI.SystemTray">

    <ContextMenu x:Shared="false" x:Key="SysTrayMenu">
        <MenuItem Header="Enable" Command="{Binding EnableScreenReadingCommand}" />
        <MenuItem Header="Disable" Command="{Binding DisableScreenReadingCommand}" />
        <MenuItem Header="Settings" Command="{Binding ShowSettingsWindowCommand}" />
        <Separator />
        <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}" />
    </ContextMenu>

    <tb:TaskbarIcon x:Key="NotifyIcon"
                    ToolTipText="Double-click for window, right-click for menu"
                    DoubleClickCommand="{Binding ShowWindowCommand}"
                    ContextMenu="{StaticResource SysTrayMenu}">
    </tb:TaskbarIcon>

</ResourceDictionary>