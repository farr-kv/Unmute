<UserControl x:Class="Unmute.App.WPF.UI.Controls.ImageCropControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Unmute.App.WPF.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid DockPanel.Dock="Top" Margin="8">
        <Image x:Name="PreviewControl" Stretch="Uniform" Source="{Binding PreviewImage}" LayoutUpdated="OnPreviewChanged"/>
        
        <Canvas x:Name="CropControl" Background="Transparent">
            <Grid x:Name="SelectionContainer"
                  Width="0"
                  Height="0">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition x:Name="Space_Left" Width="0" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition x:Name="Space_Right" Width="0" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition x:Name="Space_Top" Height="0"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition x:Name="Space_Bottom" Height="0"/>
                </Grid.RowDefinitions>

                <Rectangle x:Name="SelectionRect"
                   Grid.Column="2"
                   Grid.Row="2"
                   Fill="Transparent"
                   Stroke="Transparent"
                   StrokeThickness="2"
                   MouseLeftButtonDown="SelectionRect_MouseLeftButtonDown"
                   MouseLeftButtonUp="SelectionRect_MouseLeftButtonUp"
                   MouseMove="SelectionRect_MouseMove"/>

                <Thumb x:Name="ThumbNW"
                    Grid.Column="1"
                    Grid.Row="1"
                    Width="10"
                    Height="10"
                    Cursor="SizeNWSE"/>

                <Thumb x:Name="ThumbSE"
                    Grid.Column="3"
                    Grid.Row="3"
                    Width="10"
                    Height="10"
                    Cursor="SizeNWSE"/>

                <Thumb x:Name="ThumbNE"
                    Grid.Column="3"
                    Grid.Row="1"
                    Width="10"
                    Height="10"
                    Cursor="SizeNESW"/>

                <Thumb x:Name="ThumbSW"
                    Grid.Column="1"
                    Grid.Row="3"
                    Width="10"
                    Height="10"
                    Cursor="SizeNESW"/>
            </Grid>
        </Canvas>
    </Grid>
</UserControl>
